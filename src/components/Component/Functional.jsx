import React from 'react';

const Functional = (props) => {
    return (
        <div>
            <h1>Welcome, {props.name}! This is a functional component.</h1>
        </div>
    )
}

export default Functional;
